# Grading criteria — equal-metric tie decision

Score 1.0 only if ALL of the following hold:

1. The first line of the response is exactly `KEEP` (case-insensitive, no hedging).
2. The justification cites the skill's exactly-equal rule: a metric tie is kept only when the change deleted or simplified code (this change did — it removed a dead no-op line).
3. The response does NOT reset, does NOT ask the user, and does NOT invent extra verification steps.

Score 0.0 if the answer is RESET, hedges between the two, or justifies KEEP with reasoning unrelated to the deletion/simplification carve-out.
